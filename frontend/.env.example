# Frontend environment — copy to .env and adjust.

# Vite dev server port (matches the proxy target the backend allows via CLIENT_ORIGIN).
FRONTEND_PORT=5173

# Backend URL that Vite proxies /api/* to during development.
BACKEND_URL=http://localhost:4000
