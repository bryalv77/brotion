/**
 * Barrel export for @notion-clone/shared.
 *
 * Import from the package root in apps: `import { PageDTO, BlockContent } from "@notion-clone/shared"`.
 * Per-file imports (`@notion-clone/shared/api-types`) also work for finer granularity.
 */
export * from "./api-types.js";
export * from "./block-schema.js";
